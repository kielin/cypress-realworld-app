pipelin {
    agent any

    stages {
        stage('init') {
            steps {
                echo 'init'
            }

        stage('test') {
            steps {
                echo 'test'
            }
        }

        stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
    }
}
